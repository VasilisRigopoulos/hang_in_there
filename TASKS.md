# Tasks — Κρεμάλα

Κάνε τα με τη σειρά. Μετά από κάθε task: **Commit + Sync**. Το μήνυμα του commit ξεκινά με τον αριθμό του task.

## Task 0 — Τρέξ' το (0 γραμμές κώδικα)
Τρέξε `python hangman.py`, παίξε μία φορά. Βρες στον κώδικα: πού διαλέγεται η λέξη; πού μετριούνται τα λάθη;

## Task 1 — Δικές σου λέξεις
Πρόσθεσε τουλάχιστον **4 λέξεις** στη λίστα `WORDS` (αγγλικές, πεζά, χωρίς κενά). Τρέξε ξανά και δες ότι εμφανίζονται.
> Commit: `task 1: νέες λέξεις`

## Task 2 — Πόσες προσπάθειες μένουν;
Μέσα στον βρόχο, πριν το `input`, τύπωσε πόσα λάθη απομένουν, π.χ. `Απομένουν: 4`.
Χρησιμοποίησε τις μεταβλητές `MAX_MISTAKES` και `mistakes`.
> Commit: `task 2: εμφάνιση προσπαθειών`

## Task 3 — Ποια γράμματα έχω πει;
Στην ίδια γραμμή με το Task 2, τύπωσε και τα γράμματα που έχουν ήδη ειπωθεί, χωρισμένα με κόμμα, π.χ. `Είπες: a, e, s`.
Υπόδειξη: `", ".join(guessed)`.
> Commit: `task 3: γράμματα που είπα`

## Task 4 — Μόνο ένα γράμμα!
Αν ο παίκτης γράψει κάτι που δεν είναι **ακριβώς ένα γράμμα** (π.χ. `ab`, `5`, τίποτα), τύπωσε `Ένα γράμμα μόνο!` και ζήτα ξανά, **χωρίς** να μετρήσει ως λάθος.
Γράψε το ως ξεχωριστή συνάρτηση `valid_letter(guess)` που επιστρέφει `True`/`False`. Υπόδειξη: `len()` και `str.isalpha()`.
> Commit: `task 4: έλεγχος εισόδου`

## Task 5 — Ξανά;
Κάνε το παιχνίδι να ρωτάει στο τέλος `Ξανά; (ν/ο):`. Αν απαντήσει `ν`, ξεκινά νέος γύρος. Κράτα και **σκορ** (νίκες/γύροι) και τύπωσέ το στο τέλος, π.χ. `Νίκες: 2 από 3`.
Υπόδειξη: φτιάξε συνάρτηση `main()` με `while True:` που καλεί την `play()` — η `play()` ήδη επιστρέφει `True`/`False`.
> Commit: `task 5: πολλοί γύροι και σκορ`

## Bonus A — Κατηγορίες
Άλλαξε τη `WORDS` σε λεξικό `{"programming": [...], "animals": [...]}`. Στην αρχή του γύρου ο παίκτης διαλέγει κατηγορία.

## Bonus B — Δυσκολία
Ρώτα στην αρχή «Δυσκολία (1-3):». Αν ο παίκτης γράψει κάτι που δεν είναι αριθμός, μην κρασάρει το πρόγραμμα: χρησιμοποίησε `try` / `except ValueError` και βάλε προεπιλογή 2. Το `MAX_MISTAKES` γίνεται 8 / 6 / 4 αντίστοιχα.

---

Πώς ξέρω ότι τελείωσα το task; Τρέχω το παιχνίδι, το βλέπω να δουλεύει, και το commit φαίνεται στο github.com.
